# Session 9: NextAuth.js Authentication & Security - Solutions

---

### Question 1: NextAuth.js Setup with Google Provider & User Details
**Task:** Create a Next.js app and set up NextAuth.js with Google Provider to display user name and email on the homepage after login.

**`src/pages/api/auth/[...nextauth].js`:**
```javascript
import NextAuth from 'next-auth';
import GoogleProvider from 'next-auth/providers/google';

export const authOptions = {
  providers: [
    GoogleProvider({
      clientId: process.env.GOOGLE_CLIENT_ID,
      clientSecret: process.env.GOOGLE_CLIENT_SECRET,
    }),
  ],
  secret: process.env.NEXTAUTH_SECRET,
};

export default NextAuth(authOptions);
```

**`src/pages/_app.js` Wrapper:**
```jsx
import { SessionProvider } from 'next-auth/react';

export default function App({ Component, pageProps: { session, ...pageProps } }) {
  return (
    <SessionProvider session={session}>
      <Component {...pageProps} />
    </SessionProvider>
  );
}
```

---

### Question 2: Environment Variables (`.env.local`)
**Task:** Add `.env.local` to store Google Client ID and Secret, updating NextAuth configuration.

**`.env.local`:**
```env
GOOGLE_CLIENT_ID="mock-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="mock-google-client-secret-key"
NEXTAUTH_SECRET="super-secret-nextauth-token-key-998877665544332211"
NEXTAUTH_URL="http://localhost:3000"
```

---

### Question 3: Protected API Route (`/api/secret`)
**Task:** Protect `/api/secret` so only authenticated users can access it, returning `401 Unauthorized` for unauthenticated requests.

**`src/pages/api/secret.js`:**
```javascript
import { getServerSession } from 'next-auth/next';
import { authOptions } from './auth/[...nextauth]';

export default async function handler(req, res) {
  const session = await getServerSession(req, res, authOptions);

  // Return 401 Unauthorized if no active session exists
  if (!session) {
    return res.status(401).json({
      error: '401 Unauthorized: Access denied. Please log in first.'
    });
  }

  // Success response for authenticated user
  return res.status(200).json({
    message: 'Access Granted: Welcome to the secret protected route!',
    user: session.user.name,
    timestamp: new Date().toISOString()
  });
}
```

---

### Question 4: Protected Profile Page (`src/pages/profile.js`)
**Task:** Build a Profile page that displays user details if session is valid, or redirects to login if unauthenticated using `useSession`.

**`src/pages/profile.js`:**
```jsx
import { useSession, signOut } from 'next-auth/react';
import { useRouter } from 'next/router';
import { useEffect } from 'react';

export default function Profile() {
  const { data: session, status } = useSession();
  const router = useRouter();

  useEffect(() => {
    if (status === 'unauthenticated') {
      router.push('/api/auth/signin');
    }
  }, [status, router]);

  if (status === 'loading') return <div>Loading session status...</div>;
  if (!session) return null;

  return (
    <div>
      <h1>User Profile Page</h1>
      <h2>Welcome, {session.user.name}</h2>
      <p>Email: {session.user.email}</p>
      <button onClick={() => signOut({ callbackUrl: '/' })}>Log Out</button>
    </div>
  );
}
```

---

### Question 5: ChatGPT Generated Logout Function
**Task:** Use ChatGPT to generate a logout function that logs out the user and redirects to homepage.

**AI Tool Used:** **ChatGPT (GPT-4o)**

**Generated Code Snippet:**
```javascript
import { signOut } from 'next-auth/react';

// Logout handler function generated by ChatGPT
const handleLogout = () => {
  signOut({ callbackUrl: '/' });
};
```
**Explanation:** `signOut()` clears the active NextAuth session cookies and triggers an automatic browser navigation to the specified `callbackUrl` (`'/'`), safely logging out the user.
